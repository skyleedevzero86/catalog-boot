<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sleekydz86.catalog.adapter.outbound.persistence.category.MetaTableCategoryCommandMapper">

  <select id="selectCategoryById" resultType="com.sleekydz86.catalog.adapter.outbound.persistence.category.MetaTableCategoryRow">
    select * from t_mtdt_tbl_ctgr where mtdt_tbl_ctgr_id = #{mtdtTblCtgrId}
  </select>

  <select id="selectCategoriesByMtdtId" resultType="com.sleekydz86.catalog.adapter.outbound.persistence.category.MetaTableCategoryRow">
    select * from t_mtdt_tbl_ctgr
    where mtdt_id = #{mtdtId}
    order by sort_no asc, ctgr_nm asc
  </select>

  <select id="countChildCategories" resultType="long">
    select count(1) from t_mtdt_tbl_ctgr where up_mtdt_tbl_ctgr_id = #{mtdtTblCtgrId}
  </select>

  <select id="selectMappedTableIds" resultType="string">
    select mtdt_tbl_id from t_mtdt_tbl_ctgr_mpng
    where mtdt_tbl_ctgr_id = #{mtdtTblCtgrId}
    order by sort_no asc, mtdt_tbl_id asc
  </select>

  <update id="deleteMappingsByCategoryId" databaseId="PostgreSQL" statementType="CALLABLE">
    {call sp_mtdt_tbl_ctgr_mpng_clear(#{mtdtTblCtgrId, mode=IN, jdbcType=VARCHAR})}
  </update>

  <update id="executeMtdtTblCtgr" databaseId="PostgreSQL" statementType="CALLABLE">
    {call sp_mtdt_tbl_ctgr(
      #{op, mode=IN, jdbcType=CHAR},
      #{mtdtTblCtgrId, mode=INOUT, jdbcType=VARCHAR},
      #{mtdtId, mode=IN, jdbcType=VARCHAR},
      #{upMtdtTblCtgrId, mode=IN, jdbcType=VARCHAR},
      #{ctgrNm, mode=IN, jdbcType=VARCHAR},
      #{ctgrExpln, mode=IN, jdbcType=VARCHAR},
      #{sortNo, mode=IN, jdbcType=INTEGER},
      #{expsrYn, mode=IN, jdbcType=BOOLEAN},
      #{lwrCtgrPrmYn, mode=IN, jdbcType=BOOLEAN},
      #{actorId, mode=IN, jdbcType=VARCHAR}
    )}
  </update>

  <update id="executeMtdtTblCtgrMpng" databaseId="PostgreSQL" statementType="CALLABLE">
    {call sp_mtdt_tbl_ctgr_mpng(
      #{op, mode=IN, jdbcType=CHAR},
      #{mtdtTblCtgrMpngId, mode=INOUT, jdbcType=VARCHAR},
      #{mtdtId, mode=IN, jdbcType=VARCHAR},
      #{mtdtTblCtgrId, mode=IN, jdbcType=VARCHAR},
      #{mtdtTblId, mode=IN, jdbcType=VARCHAR},
      #{sortNo, mode=IN, jdbcType=INTEGER},
      #{expsrYn, mode=IN, jdbcType=BOOLEAN},
      #{actorId, mode=IN, jdbcType=VARCHAR}
    )}
  </update>

</mapper>
